<% provide(:title, @team.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
       <h1>
         <%= @team.name %>
         
      </h1>
    </section>
  </aside>
</div>

<% if current_user.admin? %>
<%= link_to "Add team members", new_team_membership_path(team_id:@team.id) %>
<% end %>
